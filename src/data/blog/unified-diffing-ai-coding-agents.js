
import coverUnifiedDiff from '../../assets/blog/unified-diffing-ai-coding-agents-hero.png'

const body = [
  "An AI coding agent can say an edit is complete in one sentence. That sentence is not proof. Unified diffing makes the work inspectable: a compact patch that shows precise removals, additions, and enough nearby code to judge the change in context.",
  "A confident completion message can hide a wrong file, an unnecessary rewrite, or a subtle behaviour change. A readable diff creates a controlled hand-off between automation and review.",
  "## Unified diffing: the direct answer",
  "A unified diff is a line-based representation of the difference between two file versions. It uses file headers, hunk locations, unchanged context, minus-prefixed removals, and plus-prefixed additions. Coding agents use this format because it is concise, machine-applicable, and much easier to audit than a full rewritten file.",
  "At a glance:",
  "- Read the file path before reading the code.\n- Treat each @@ block as a separate change area, or hunk.\n- Lines beginning with - are removed; lines beginning with + are added.\n- Unprefixed context lines show where the change sits.\n- A clean diff improves reviewability, but tests still decide whether behaviour is correct.",
  "## The patch is the evidence",
  "A completion message is a summary. The unified diff is evidence. Imagine an agent changing a Markdown image from a utility class that limits width to an inline style with a fixed width of 400 pixels. The patch tells you where the edit happened, what left, what arrived, and how broad the edit was.",
  "It does not prove that 400 pixels is the right responsive choice. It proves what the agent changed. That distinction is the foundation of reliable agent-assisted development.",
  "## What unified diffing actually is",
  "A diff compares two versions of text. The unified format places removals and additions in one inline block rather than showing complete files side by side. It keeps changed lines plus a small amount of nearby context, making it practical for terminals, pull requests, patch files, and coding-agent interfaces.",
  "The [GNU Diffutils documentation](https://www.gnu.org/software/diffutils/manual/html_node/Unified-Format.html) describes unified output as a compact context format. By default it includes three context lines. Traditional output begins with two file headers: --- for the old file and +++ for the new file.",
  "Git commonly adds an extended header such as diff --git a/file b/file. Its [diff format documentation](https://git-scm.com/docs/diff-format) also covers metadata for modes, renames, copies, and object IDs, because a code change can involve more than text.",
  "## Read the five signals in order",
  "Read a patch from file path to hunk context before judging individual additions.",
  "1. **Git header.** Identifies the old and new paths.\n2. **File headers.** --- points to the previous version; +++ points to the proposed version.\n3. **Hunk header.** @@ -656,7 +656,7 @@ identifies the affected range in each version.\n4. **Context lines.** Lines beginning with a space are unchanged. They show where the edit sits.\n5. **Change lines.** A leading - removes content; a leading + adds content.",
  "The hunk header is mechanical. -656,7 means the old block starts near line 656 and spans seven lines. +656,7 describes the corresponding new range. If a range contains a single line, the count can be omitted.",
  "> Follow the same sequence every time: file, hunk, context, removal, addition. It works even when your diff viewer has no colour.",
  "## Why coding agents work well with patches",
  "An agent often makes a narrow change inside a much larger file. Reviewing an entire rewritten file buries the useful information. A diff reduces the review surface to the intended delta.",
  "### Scope becomes visible",
  "A request might be to make a screenshot smaller, but the agent could touch a shared component, a global stylesheet, or several unrelated files. The changed-path list and its hunks make that expansion visible before you accept it.",
  "### The patch can be checked",
  "A unified patch is structured enough for tooling. Git can test whether a patch applies without changing files by using git apply --check change.patch. Git documents [--check](https://git-scm.com/docs/git-apply) as an applicability and error check. It is useful, but it is not a behaviour test.",
  "### Review stays focused on intent",
  "The reviewer can compare the request directly with the delta. If the task is to change width, a one-line styling edit is easy to reason about. If the patch also renames variables or reformats the file, the extra work is visible as noise or scope creep.",
  "## A practical review workflow",
  "A reliable workflow treats the diff as a review gate, not automatic approval. Separate whether the patch applies from whether the change is correct. Use the diff to verify scope and implementation, then use automated checks to verify behaviour.",
  "### 1. Restate the intended outcome",
  "Write the smallest testable version of the request. For the screenshot example: render this image at a visibly smaller width without changing other slide content. This gives the review a target. Without it, almost any plausible edit can look acceptable.",
  "### 2. Inspect every changed path",
  "Check every file before reading individual lines. Does it belong to the task? Is a shared or generated file changing unnecessarily? Is a lockfile change expected? Did the work omit a test or documentation file that should change too? Unexpected files are a reason to pause, not something to skim past.",
  "### 3. Read each hunk as a small argument",
  "Compare the removed line, added line, and surrounding context. The new code should explain how it satisfies the request. If you cannot connect a hunk to the outcome, ask the agent to justify or remove it.",
  "Watch for broad formatting churn. A one-line feature buried inside 200 reformatted lines is harder to audit and more likely to collide with another branch.",
  "### 4. Check semantic consequences",
  "Line-level precision does not guarantee design quality. In the example, width: 400px is explicit, but a fixed width can overflow a narrow container unless the renderer imposes a safe maximum. The right follow-up might be max-width: 100% alongside the desired width.",
  "The question is not merely whether the line changed. It is what the new line will do in every relevant state.",
  "### 5. Validate, test, then commit",
  "Run cheap checks first: git diff --check catches whitespace errors, while git apply --check change.patch ensures a patch applies. Then run the formatter, linter, type checker, unit tests, and a focused manual check where relevant.",
  "Review the final diff again after automated tools run. A formatter or code generator can add changes of its own.",
  "> Let the agent propose the patch. Let deterministic tools verify syntax and behaviour. Let a human decide whether the change matches the product intent.",
  "## What unified diffing cannot prove",
  "A diff is evidence of change, not evidence of correctness. It cannot tell you whether a requirement was misunderstood, whether a fixed width breaks on mobile, whether an API call is secure, or whether the edited code is the right architectural layer.",
  "It also provides only limited nearby context. A hunk can look reasonable while depending on code elsewhere in the file or repository. Generated files, minified assets, notebooks, and binary files can produce patches that are difficult or impossible to review meaningfully.",
  "Colour is a convenience, not the grammar. Accessible review tools must preserve +, -, file headers, and hunk markers so meaning does not depend on red and green alone.",
  "## Commands worth keeping close",
  "Show the working-tree diff:\n\n    git diff\n\nControl context lines:\n\n    git diff --unified=5\n\nSee a summary before detail:\n\n    git diff --stat\n    git diff --name-only\n\nCheck whitespace:\n\n    git diff --check\n\nCheck whether a patch applies:\n\n    git apply --check change.patch",
  "Python also exposes difflib.unified_diff(), which returns unified-format delta lines from two sequences. The [Python documentation](https://docs.python.org/3/library/difflib.html#difflib.unified_diff) notes that the context count defaults to three. It is useful for previews, content versioning, audit trails, configuration changes, and tools that need to show a human-readable delta before writing data.",
  "## The checklist I use before accepting a patch",
  "- The changed files match the request.\n- Every hunk has a clear reason to exist.\n- The context confirms the edit is in the right place.\n- Removed behaviour is intentional.\n- Added behaviour handles relevant states and constraints.\n- Formatting noise and unrelated refactors are absent.\n- The patch applies cleanly.\n- Linting, types, and tests pass.\n- The final diff still matches the original request.\n- The commit message explains the outcome, not just the files.",
  "## The takeaway",
  "Unified diffing is one of the simplest ways to keep control when an AI agent edits code. It compresses the work into a reviewable contract: these files, these locations, these removals, these additions.",
  "Read that contract before you trust the completion message. Then validate the code with the same checks you would use for any human-authored change.",
  "If you are designing an AI-assisted engineering workflow and want the agents, permissions, review gates, and testing strategy to work as one system, [get in touch](/contact).",
].join('\n\n')

export default {
  slug: 'unified-diffing-ai-coding-agents',
  title: 'Unified Diffing: How AI Coding Agents Show Exactly What Changed',
  excerpt:
    'A practical guide to reading agent-generated patches, spotting scope creep, and validating code changes before production.',
  tag: 'Full-Stack',
  tint: 'cyan',
  cover: coverUnifiedDiff,
  coverAlt:
    'Editorial illustration of a unified diff with removed and added lines under review through a magnifying glass.',
  ogImage: '/images/blog/unified-diffing-ai-coding-agents-og.png',
  heroLines: ['READ THE', 'PATCH.'],
  heroSubline: 'UNIFIED DIFFING FOR AI CODING AGENTS.',
  date: '2026-08-31',
  featured: true,
  author: 'Rupam Bhakta',
  seoTitle: 'Unified Diffing for AI Coding Agents | Rupam Bhakta',
  metaDescription:
    'Unified diffing makes AI code edits reviewable. Learn how to read a patch, catch scope creep, validate changes, and keep control of agent workflows.',
  body,
}

